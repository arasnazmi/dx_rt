# DXRT LIB
set(target dxrt)
if (NOT MSVC)
    if (USE_SHARED_DXRT_LIB)
        set(dxrt_lib_file "${CMAKE_CURRENT_SOURCE_DIR}/lib/libdxrt_${CMAKE_SYSTEM_PROCESSOR}.so"
            CACHE STRING "dxrt lib. file")
    else()
        set(dxrt_lib_file "${CMAKE_CURRENT_SOURCE_DIR}/lib/libdxrt_${CMAKE_SYSTEM_PROCESSOR}.a"
            CACHE STRING "dxrt lib. file")
    endif()
endif()

FILE(GLOB_RECURSE srcs "*.cpp" "*.cc" "*.c")
FILE(GLOB_RECURSE headers "include/dxrt/*.h")

# All headers consolidated in include/dxrt/ (no separate internal paths)

# C ABI (dxrt_c_api.cpp) is compiled directly into libdxrt.so

# remove data_ppcpu.cpp if it can be generated by data/CMakeLists.txt
if (EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/data/CMakeLists.txt")
    list(REMOVE_ITEM srcs "${CMAKE_CURRENT_SOURCE_DIR}/data/ppcpu_data.cpp")
endif()

# Exclude ARM-specific files on x86_64
if (CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64" OR CMAKE_SYSTEM_PROCESSOR STREQUAL "AMD64")
    list(FILTER srcs EXCLUDE REGEX "xnn_kernel\\.cpp$")
    list(FILTER headers EXCLUDE REGEX "xnn_kernel\\.h$")
    message(STATUS "Excluded ARM-specific files (xnn_kernel.cpp/h) from build on x86_64")
endif()


message("DXRT LIB Source files : " ${srcs})
message("DXRT LIB Header files : " ${headers})

# Associate all headers with the target for IDE visibility
set(all_headers ${headers})

if (MSVC)
    if (USE_SHARED_DXRT_LIB)
        add_library(${target} SHARED ${srcs} ${all_headers})
        set_target_properties(${target} PROPERTIES
            RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}\\bin"
            ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}\\lib"
        )
        # Define DLL export macro
        target_compile_definitions(${target}
            PRIVATE DXRT_EXPORTS
            PUBLIC DXRT_DLL
        )
        if(CMAKE_BUILD_TYPE MATCHES Debug)
            set_target_properties(${target} PROPERTIES OUTPUT_NAME "dxrtdbg")
        endif()
    else()
        add_library(${target} STATIC ${srcs} ${all_headers})
        set_target_properties(${target} PROPERTIES
            ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
        )
        # Define macro for static library
        target_compile_definitions(${target}
            PUBLIC DXRT_STATIC
        )
        # Internal tools link against dxrt_static; alias to dxrt for static builds
        add_library(dxrt_static ALIAS dxrt)
        set(DXRT_STATIC_IS_ALIAS TRUE)
    endif()
else()
    if (USE_SHARED_DXRT_LIB)
        add_library(dxrt_obj OBJECT ${srcs} ${all_headers})
        set_property(TARGET dxrt_obj PROPERTY POSITION_INDEPENDENT_CODE ON)

        add_library(${target} SHARED $<TARGET_OBJECTS:dxrt_obj> ${all_headers})
        target_link_libraries(${target} PRIVATE rt)
        # -Bsymbolic: Resolve C++ symbols to .so's own definitions.
        # Prevents api header classes from interposing internal symbols.
        target_link_options(${target} PRIVATE "LINKER:-Bsymbolic")
        # Always apply version script: only dxrt_* C ABI symbols exported
        target_link_options(${target} PRIVATE
            "LINKER:--version-script=${CMAKE_CURRENT_SOURCE_DIR}/dxrt_export.map")
        set_target_properties(${target} PROPERTIES
            LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
        )

        # Static library for internal tools (tests, benchmarks, CLI)
        # Reuse dxrt_obj to avoid compiling the same source set twice.
        add_library(dxrt_static STATIC $<TARGET_OBJECTS:dxrt_obj>)
        target_link_libraries(dxrt_static PRIVATE rt)
        set_target_properties(dxrt_static PROPERTIES
            OUTPUT_NAME "dxrt"
            ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
        )
        set(DXRT_STATIC_IS_ALIAS FALSE)
    else()
        add_library(${target} STATIC ${srcs} ${all_headers})
        set_property(TARGET ${target} PROPERTY POSITION_INDEPENDENT_CODE ON)
        # Internal tools link against dxrt_static; alias to dxrt for static builds
        add_library(dxrt_static ALIAS dxrt)
        set(DXRT_STATIC_IS_ALIAS TRUE)
    endif()
endif()

file(GLOB CFG_FILES ${CMAKE_CURRENT_SOURCE_DIR}/cfg/*.cfg)

foreach(cfg_file ${CFG_FILES})
    file(STRINGS ${cfg_file} cfgs)
    foreach(cfg ${cfgs})
        target_compile_definitions(${target} PUBLIC "${cfg}")
        if(TARGET dxrt_obj)
            target_compile_definitions(dxrt_obj PUBLIC "${cfg}")
        endif()
    endforeach()
endforeach()

# C ABI export visibility (needed for DXRT_CAPI macro on Windows)
if(TARGET dxrt_obj)
    target_compile_definitions(dxrt_obj PRIVATE DXRT_C_BUILDING)
else()
    target_compile_definitions(${target} PRIVATE DXRT_C_BUILDING)
endif()

# Embed the source git commit hash for dxrt-cli / dxrtd version output.
# Resolved at configure time (build.sh always re-runs cmake, so it stays fresh).
# Empty when git is unavailable (e.g. tarball build) -> output unchanged.
# Applied only to configuration.cpp so a hash change recompiles just that file.
find_package(Git QUIET)
set(DXRT_GIT_HASH "")
if(Git_FOUND)
    execute_process(
        COMMAND ${GIT_EXECUTABLE} -C ${CMAKE_SOURCE_DIR} rev-parse --short=7 HEAD
        OUTPUT_VARIABLE _dxrt_git_hash
        OUTPUT_STRIP_TRAILING_WHITESPACE
        RESULT_VARIABLE _dxrt_git_rc
        ERROR_QUIET)
    if(_dxrt_git_rc EQUAL 0)
        set(DXRT_GIT_HASH "${_dxrt_git_hash}")
        execute_process(
            COMMAND ${GIT_EXECUTABLE} -C ${CMAKE_SOURCE_DIR} diff --quiet HEAD
            RESULT_VARIABLE _dxrt_git_dirty_rc)
        if(_dxrt_git_dirty_rc EQUAL 1)
            set(DXRT_GIT_HASH "${DXRT_GIT_HASH}-dirty")
        endif()
    endif()
endif()
message(STATUS "  DXRT git hash : ${DXRT_GIT_HASH}")
set_source_files_properties(${CMAKE_CURRENT_SOURCE_DIR}/configuration.cpp
    PROPERTIES COMPILE_DEFINITIONS DXRT_GIT_HASH="${DXRT_GIT_HASH}")
target_include_directories(
    ${target}
    PRIVATE
    ${CMAKE_SOURCE_DIR}/extern/include
    ${CMAKE_SOURCE_DIR}/lib/dxrt_service
    ${CMAKE_SOURCE_DIR}/lib/device_shm
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/dynamic_ipc
    ${CMAKE_CURRENT_SOURCE_DIR}/include
    ${DXRT_INTERNAL_INCLUDE_DIRS}
)
if(TARGET dxrt_obj)
    target_include_directories(
        dxrt_obj
        PRIVATE
        ${CMAKE_SOURCE_DIR}/extern/include
        ${CMAKE_SOURCE_DIR}/lib/dxrt_service
        ${CMAKE_SOURCE_DIR}/lib/device_shm
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/dynamic_ipc
        ${CMAKE_CURRENT_SOURCE_DIR}/include
        ${DXRT_INTERNAL_INCLUDE_DIRS}
    )
endif()

# dxrt_static: same include dirs as dxrt (ALIAS inherits automatically)
if(TARGET dxrt_static AND NOT DXRT_STATIC_IS_ALIAS)
    target_include_directories(dxrt_static PUBLIC
        ${CMAKE_SOURCE_DIR}/extern/include
        ${CMAKE_SOURCE_DIR}/lib/dxrt_service
        ${CMAKE_SOURCE_DIR}/lib/device_shm
        ${CMAKE_CURRENT_SOURCE_DIR}/include
    )
    target_include_directories(dxrt_static PRIVATE ${DXRT_INTERNAL_INCLUDE_DIRS})
    target_compile_definitions(dxrt_static PRIVATE DXRT_C_BUILDING)
endif()

if (MSVC AND USE_ORT)
file(GLOB ORT_DLL_DLLS "${onnxruntime_LIB_DIRS}/*.dll")
foreach(ORT_DLL_DLL IN LISTS ORT_DLL_DLLS)
    add_custom_command(
                TARGET ${target} POST_BUILD
                COMMAND ${CMAKE_COMMAND} -E copy_if_different
                ${ORT_DLL_DLL}
                ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
endforeach()
endif()

if (USE_ORT)
    target_include_directories(${target} PUBLIC
        ${onnxruntime_INCLUDE_DIRS}
    )
    if(TARGET dxrt_obj)
        target_include_directories(dxrt_obj PUBLIC
            ${onnxruntime_INCLUDE_DIRS}
        )
    endif()
    target_link_directories(${target} PUBLIC ${ONNXLIB_DIRS})
    target_link_libraries(${target} PUBLIC ${ONNXLIB_DIRS})
    if (MSVC)
        # Delay-load onnxruntime.dll so that our hook (ort_delay_load.cpp) can
        # force it to be loaded from the same directory as dxrt.dll, avoiding
        # a stale copy in C:\Windows\System32 being picked up first.
        target_link_options(${target} PRIVATE "/DELAYLOAD:onnxruntime.dll")
        target_link_libraries(${target} PRIVATE delayimp)
    endif()
endif()

if(TARGET dxrt_static AND NOT DXRT_STATIC_IS_ALIAS AND USE_ORT)
    target_include_directories(dxrt_static PUBLIC ${onnxruntime_INCLUDE_DIRS})
    target_link_directories(dxrt_static PUBLIC ${ONNXLIB_DIRS})
    target_link_libraries(dxrt_static PUBLIC ${ONNXLIB_DIRS})
endif()

if (USE_VNPU)
    # Link Rockchip MPI library for ARM platforms
    if (CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64" OR CMAKE_SYSTEM_PROCESSOR MATCHES "arm")
        set(EXTERNAL_DIR ${CMAKE_SOURCE_DIR}/external)
        set(EP_LIB_PATH ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/ep/lib)
        set(EXTERN_LIB_PATH ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/extern/lib)

        target_include_directories(${target} PUBLIC
            ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/ep/include
            ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/extern/include/mpi
        )
        if(TARGET dxrt_obj)
            target_include_directories(dxrt_obj PUBLIC
                ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/ep/include
                ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/extern/include/mpi
            )
        endif()

        # Explicitly add link directory
        link_directories(${EP_LIB_PATH})
        link_directories(${EXTERN_LIB_PATH})
        target_link_directories(${target} PUBLIC ${EP_LIB_PATH})
        target_link_directories(${target} PUBLIC ${EXTERN_LIB_PATH})

        find_library(ROCKIT_LIB rockit PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(ROCKCHIP_MPP_LIB rockchip_mpp PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(DX_VNPU_CORE_EP_LIB dx_vnpu_core_ep PATHS ${EP_LIB_PATH} NO_DEFAULT_PATH)
        find_library(DRM_LIB drm PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(ASOUND_LIB asound PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(AVFORMAT_LIB avformat PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(AVCODEC_LIB avcodec PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(AVUTIL_LIB avutil PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)
        find_library(SWRESAMPLE_LIB swresample PATHS ${EXTERN_LIB_PATH} NO_DEFAULT_PATH)

        # Verify required libraries are found
        if (NOT ROCKIT_LIB)
            message(FATAL_ERROR "librockit.so not found in ${EXTERN_LIB_PATH} - Required for ARM platform")
        endif()

        if (NOT ROCKCHIP_MPP_LIB)
            message(FATAL_ERROR "librockchip_mpp.so not found in ${EXTERN_LIB_PATH} - Required for ARM platform")
        endif()

        if (NOT DX_VNPU_CORE_EP_LIB)
            message(FATAL_ERROR "libdx_vnpu_core_ep.so not found in ${EP_LIB_PATH} - Required for ARM platform")
        endif()

        message(STATUS "Found librockit: ${ROCKIT_LIB}")
        message(STATUS "Found librockchip_mpp: ${ROCKCHIP_MPP_LIB}")
        message(STATUS "Found libdx_vnpu_core_ep: ${DX_VNPU_CORE_EP_LIB}")

        # Use names instead of full paths to ensure proper linking
        target_link_libraries(${target} PUBLIC
            rockit
            rockchip_mpp
            dx_vnpu_core_ep
            drm
            asound
            avformat
            avcodec
            avutil
            swresample
        )
        target_compile_definitions(${target} PUBLIC USE_ROCKCHIP_MPI)
        if(TARGET dxrt_obj)
            target_compile_definitions(dxrt_obj PUBLIC USE_ROCKCHIP_MPI)
        endif()
    endif()

endif() # USE_VNPU

if(TARGET dxrt_static AND NOT DXRT_STATIC_IS_ALIAS AND USE_VNPU)
    if (CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64" OR CMAKE_SYSTEM_PROCESSOR MATCHES "arm")
        target_include_directories(dxrt_static PUBLIC
            ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/ep/include
            ${EXTERNAL_DIR}/dx_vnpu_core_lib/linux/extern/include/mpi
        )
        target_link_directories(dxrt_static PUBLIC ${EP_LIB_PATH})
        target_link_directories(dxrt_static PUBLIC ${EXTERN_LIB_PATH})
        target_link_libraries(dxrt_static PUBLIC
            rockit rockchip_mpp dx_vnpu_core_ep
            drm asound avformat avcodec avutil swresample
        )
        target_compile_definitions(dxrt_static PUBLIC USE_ROCKCHIP_MPI)
    endif()
endif()

# ============================================================================
# NPU Format Conversion Acceleration: detect libraries, validate environment, link
# ============================================================================
if (USE_NPU_FORMAT_CONVERSION_ACCELERATION)
    if (CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64" OR CMAKE_SYSTEM_PROCESSOR STREQUAL "AMD64")
        # x86_64: Use Intel IPP — find the library
        if(DEFINED ENV{IPPROOT})
            set(IPP_ROOT "$ENV{IPPROOT}")
        elseif(MSVC)
            if(EXISTS "C:/Program Files (x86)/Intel/oneAPI/ipp/latest")
                set(IPP_ROOT "C:/Program Files (x86)/Intel/oneAPI/ipp/latest")
            elseif(EXISTS "C:/Program Files/Intel/oneAPI/ipp/latest")
                set(IPP_ROOT "C:/Program Files/Intel/oneAPI/ipp/latest")
            elseif(EXISTS "C:/Intel/oneAPI/ipp/latest")
                set(IPP_ROOT "C:/Intel/oneAPI/ipp/latest")
            endif()
        else()
            if(EXISTS "/opt/intel/oneapi/ipp/latest")
                set(IPP_ROOT "/opt/intel/oneapi/ipp/latest")
            elseif(EXISTS "/opt/intel/ipp")
                set(IPP_ROOT "/opt/intel/ipp")
            endif()
        endif()

        set(IPP_FOUND FALSE)
        if(IPP_ROOT)
            set(IPP_INCLUDE_DIR "${IPP_ROOT}/include")
            set(IPP_LIB_DIR "${IPP_ROOT}/lib/intel64")
            if(EXISTS "${IPP_INCLUDE_DIR}/ipp.h" AND EXISTS "${IPP_LIB_DIR}")
                message(STATUS "Intel IPP found at: ${IPP_ROOT}")
                set(IPP_FOUND TRUE)
                if(MSVC)
                    set(IPP_LIBRARIES
                        "${IPP_LIB_DIR}/ippi.lib"
                        "${IPP_LIB_DIR}/ipps.lib"
                        "${IPP_LIB_DIR}/ippcore.lib")
                else()
                    set(IPP_LIBRARIES
                        "${IPP_LIB_DIR}/libippi.a"
                        "${IPP_LIB_DIR}/libipps.a"
                        "${IPP_LIB_DIR}/libippcore.a")
                endif()
            else()
                message(WARNING "Intel IPP header or libraries not found at ${IPP_ROOT}. NFH acceleration will be disabled.")
            endif()
        else()
            message(WARNING "Intel IPP root not found. Set IPPROOT environment variable or install Intel oneAPI. NFH acceleration will be disabled.")
        endif()

        if (IPP_FOUND)
            target_include_directories(${target} PUBLIC ${IPP_INCLUDE_DIR})
            target_link_libraries(${target} PUBLIC ${IPP_LIBRARIES})
            target_compile_definitions(${target} PUBLIC USE_IPP)
            if(TARGET dxrt_obj)
                target_include_directories(dxrt_obj PUBLIC ${IPP_INCLUDE_DIR})
                target_compile_definitions(dxrt_obj PUBLIC USE_IPP)
            endif()
            message(STATUS "Intel IPP enabled for dxrt library (x86_64)")
        else()
            message(WARNING "IPP not found. NPU format conversion acceleration will use fallback implementation on x86_64")
            set(USE_NPU_FORMAT_CONVERSION_ACCELERATION OFF CACHE BOOL "Accelerate NPU data format conversion (transpose/padding)" FORCE)
        endif()

    elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64|arm64|ARM64)$")
        # ARM64 (ARMv8): NEON/ASIMD is always available (built-in)
        set(ARM_NEON_FOUND TRUE)
        target_compile_definitions(${target} PUBLIC USE_NEON)
        if(TARGET dxrt_obj)
            target_compile_definitions(dxrt_obj PUBLIC USE_NEON)
        endif()
        message(STATUS "ARM NEON/ASIMD enabled for NFH acceleration (${CMAKE_SYSTEM_PROCESSOR})")

    else()
        message(WARNING "Unsupported architecture for NPU format conversion acceleration: ${CMAKE_SYSTEM_PROCESSOR}. Disabling.")
        set(USE_NPU_FORMAT_CONVERSION_ACCELERATION OFF CACHE BOOL "Accelerate NPU data format conversion (transpose/padding)" FORCE)
    endif()
endif()

if (USE_NPU_FORMAT_CONVERSION_ACCELERATION AND FORCE_NPU_FORMAT_CONVERSION_ACCELERATION)
    target_compile_definitions(${target} PUBLIC FORCE_NFH_ACCELERATION)
    if(TARGET dxrt_obj)
        target_compile_definitions(dxrt_obj PUBLIC FORCE_NFH_ACCELERATION)
    endif()
    message(STATUS "FORCE_NPU_FORMAT_CONVERSION_ACCELERATION enabled - env var check will be skipped at runtime")
endif()

# ============================================================================
# CPU Op Acceleration: glibc check, detect libraries, validate, link
# ============================================================================
# Pre-built ORT with acceleration EPs has glibc requirements:
#   x86_64: built on Ubuntu 22.04 (glibc >= 2.35)
#   aarch64: built on Debian 11 (glibc >= 2.31)
if(USE_CPU_OP_ACCELERATION AND NOT WIN32)
    execute_process(
        COMMAND ldd --version
        OUTPUT_VARIABLE _LDD_OUTPUT
        ERROR_VARIABLE _LDD_OUTPUT
        OUTPUT_STRIP_TRAILING_WHITESPACE
    )
    string(REGEX MATCH "[0-9]+\\.[0-9]+" _GLIBC_VERSION "${_LDD_OUTPUT}")
    if(_GLIBC_VERSION)
        message(STATUS "Detected glibc version: ${_GLIBC_VERSION}")
        if(CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64" AND _GLIBC_VERSION VERSION_LESS "2.35")
            message(WARNING "glibc ${_GLIBC_VERSION} < 2.35. CPU op acceleration on x86_64 requires glibc >= 2.35 (Ubuntu 22.04+). Disabling.")
            set(USE_CPU_OP_ACCELERATION OFF CACHE BOOL "Accelerate CPU-side ONNX operations" FORCE)
        elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64|arm64)$" AND _GLIBC_VERSION VERSION_LESS "2.31")
            message(WARNING "glibc ${_GLIBC_VERSION} < 2.31. CPU op acceleration on aarch64 requires glibc >= 2.31 (Ubuntu 20.04+/Debian 11+). Disabling.")
            set(USE_CPU_OP_ACCELERATION OFF CACHE BOOL "Accelerate CPU-side ONNX operations" FORCE)
        endif()
    else()
        message(WARNING "Could not detect glibc version. CPU Task acceleration compatibility unknown.")
    endif()
    unset(_LDD_OUTPUT)
    unset(_GLIBC_VERSION)
endif()

if (USE_CPU_OP_ACCELERATION)
    if (CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64" OR CMAKE_SYSTEM_PROCESSOR STREQUAL "AMD64")
        # x86_64: Try to find OpenVINO for CPU op acceleration
        message(STATUS "x86_64 architecture detected. Searching for OpenVINO...")
        find_package(OpenVINO QUIET COMPONENTS Runtime)

        if(OpenVINO_FOUND)
            message(STATUS "OpenVINO found - CPU op acceleration enabled with OpenVINO")
            target_link_libraries(${target} PUBLIC openvino::runtime)
            target_compile_definitions(${target} PUBLIC USE_OPENVINO)
            if(TARGET dxrt_obj)
                target_compile_definitions(dxrt_obj PUBLIC USE_OPENVINO)
            endif()
            message(STATUS "OpenVINO library linked for CPU op acceleration (x86_64)")
        else()
            message(WARNING "OpenVINO not found - CPU op acceleration will be disabled on x86_64")
            message(WARNING "  Install OpenVINO toolkit or set OpenVINO_DIR environment variable")
            set(USE_CPU_OP_ACCELERATION OFF CACHE BOOL "Accelerate CPU-side ONNX operations" FORCE)
        endif()

    elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64|arm64|ARM64)$")
        # ARM: Enable XNNPACK EP for CPU Task acceleration (via ONNX Runtime)
        if(NOT DEFINED ARM_NEON_FOUND)
            set(ARM_NEON_FOUND TRUE)
            message(STATUS "ARM NEON/ASIMD enabled for CPU Task (mandatory on ARMv8/aarch64)")
        else()
            message(STATUS "Reusing NEON detection from NFH acceleration for CPU Task")
        endif()
        target_compile_definitions(${target} PUBLIC USE_XNNPACK)
        if(TARGET dxrt_obj)
            target_compile_definitions(dxrt_obj PUBLIC USE_XNNPACK)
        endif()
        message(STATUS "XNNPACK EP enabled for CPU Task acceleration (${CMAKE_SYSTEM_PROCESSOR})")

    else()
        message(WARNING "Unsupported architecture for CPU op acceleration: ${CMAKE_SYSTEM_PROCESSOR}. Disabling.")
        set(USE_CPU_OP_ACCELERATION OFF CACHE BOOL "Accelerate CPU-side ONNX operations" FORCE)
    endif()
endif()

if (USE_CPU_OP_ACCELERATION AND FORCE_CPU_OP_ACCELERATION)
    target_compile_definitions(${target} PUBLIC FORCE_CPU_TASK_ACCELERATION)
    if(TARGET dxrt_obj)
        target_compile_definitions(dxrt_obj PUBLIC FORCE_CPU_TASK_ACCELERATION)
    endif()
    message(STATUS "FORCE_CPU_OP_ACCELERATION enabled - env var check will be skipped at runtime")
endif()

# if (USE_PYTHON)
#     set_target_properties(${target} PROPERTIES
#         POSITION_INDEPENDENT_CODE ON
#         PUBLIC_HEADER "${dxrt_lib_public_headers}"
#     )
# else()
#     set_target_properties(${target} PROPERTIES
#         PUBLIC_HEADER "${dxrt_lib_public_headers}"
#     OUTPUT_NAME "dxrt_${CMAKE_SYSTEM_PROCESSOR}"
#     )
# endif()

# Propagate acceleration compile definitions into gen.h for out-of-tree builds (e.g. Python package)
get_target_property(_dxrt_compile_defs ${target} COMPILE_DEFINITIONS)
set(USE_IPP_DEFINE "")
set(USE_NEON_DEFINE "")
set(USE_OPENVINO_DEFINE "")
set(USE_XNNPACK_DEFINE "")
set(DXRT_NFH_ACCELERATION_AVAILABLE_DEFINE "")
set(DXRT_CPU_OP_ACCELERATION_AVAILABLE_DEFINE "")
if(_dxrt_compile_defs)
    if("USE_IPP" IN_LIST _dxrt_compile_defs)
        set(USE_IPP_DEFINE "#ifndef USE_IPP\n#define USE_IPP\n#endif")
    endif()
    if("USE_NEON" IN_LIST _dxrt_compile_defs)
        set(USE_NEON_DEFINE "#ifndef USE_NEON\n#define USE_NEON\n#endif")
    endif()
    if("USE_OPENVINO" IN_LIST _dxrt_compile_defs)
        set(USE_OPENVINO_DEFINE "#ifndef USE_OPENVINO\n#define USE_OPENVINO\n#endif")
    endif()
    if("USE_XNNPACK" IN_LIST _dxrt_compile_defs)
        set(USE_XNNPACK_DEFINE "#ifndef USE_XNNPACK\n#define USE_XNNPACK\n#endif")
    endif()
    # Derived availability macros for public API consumers (e.g. run_model CLI)
    if("USE_IPP" IN_LIST _dxrt_compile_defs OR "USE_NEON" IN_LIST _dxrt_compile_defs)
        set(DXRT_NFH_ACCELERATION_AVAILABLE_DEFINE "#ifndef DXRT_NFH_ACCELERATION_AVAILABLE\n#define DXRT_NFH_ACCELERATION_AVAILABLE\n#endif")
    endif()
    if("USE_OPENVINO" IN_LIST _dxrt_compile_defs OR "USE_XNNPACK" IN_LIST _dxrt_compile_defs)
        set(DXRT_CPU_OP_ACCELERATION_AVAILABLE_DEFINE "#ifndef DXRT_CPU_OP_ACCELERATION_AVAILABLE\n#define DXRT_CPU_OP_ACCELERATION_AVAILABLE\n#endif")
    endif()
endif()
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/dxrt/gen.h.in ${CMAKE_CURRENT_SOURCE_DIR}/include/dxrt/gen.h @ONLY)

if (EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/data/CMakeLists.txt")
    add_subdirectory(data)
endif()
add_subdirectory(bindings)

include(CMakePackageConfigHelpers)

# dxrt_static: copy ALL compile definitions and acceleration libraries from dxrt.
# This runs after all definitions/link-libraries are set (IPP, NEON, OpenVINO, XNNPACK, etc.)
# dxrt_static: copy ALL compile definitions and acceleration libraries from dxrt.
# This runs after all definitions/link-libraries are set (IPP, NEON, OpenVINO, XNNPACK, etc.)
if(TARGET dxrt_static AND NOT DXRT_STATIC_IS_ALIAS)
    get_target_property(_dxrt_all_defs ${target} COMPILE_DEFINITIONS)
    if(_dxrt_all_defs)
        target_compile_definitions(dxrt_static PUBLIC ${_dxrt_all_defs})
    endif()
    # Acceleration libraries
    if(USE_NPU_FORMAT_CONVERSION_ACCELERATION AND IPP_FOUND)
        target_include_directories(dxrt_static PUBLIC ${IPP_INCLUDE_DIR})
        target_link_libraries(dxrt_static PUBLIC ${IPP_LIBRARIES})
    endif()
    if(USE_CPU_OP_ACCELERATION AND OpenVINO_FOUND)
        target_link_libraries(dxrt_static PUBLIC openvino::runtime)
    endif()
endif()

get_property(_extra_objs GLOBAL PROPERTY DXRT_EXTRA_OBJECTS)
if(_extra_objs)
    target_sources(dxrt PRIVATE ${_extra_objs})
    if(TARGET dxrt_static AND NOT DXRT_STATIC_IS_ALIAS)
        target_sources(dxrt_static PRIVATE ${_extra_objs})
    endif()
endif()

configure_package_config_file(
    ${CMAKE_CURRENT_SOURCE_DIR}/config.cmake.in
    ${CMAKE_CURRENT_BINARY_DIR}/dxrtConfig.cmake
    INSTALL_DESTINATION lib/cmake/dxrt
)
write_basic_package_version_file(
    ${CMAKE_CURRENT_BINARY_DIR}/dxrtConfigVersion.cmake
    VERSION ${release_ver}
    COMPATIBILITY SameMajorVersion
)

if(MSVC)
    install(
        TARGETS ${target}
        EXPORT ${target}
        RUNTIME DESTINATION bin
        ARCHIVE DESTINATION lib
        PUBLIC_HEADER DESTINATION include/dxrt
    )
else()
    install(
        TARGETS ${target}
        EXPORT ${target}
        RUNTIME DESTINATION bin
        LIBRARY DESTINATION lib
        PUBLIC_HEADER DESTINATION include/dxrt
    )
endif()

# ===========================================================================
# SDK Install — DXRT_PUBLIC_HEADERS (ORT-style explicit list)
# ===========================================================================
set(DXRT_INCLUDE ${CMAKE_CURRENT_SOURCE_DIR}/include)

# Remove stale headers from previous builds before installing.
# CI runners reuse /usr/local, so old internal headers (e.g. original model.h)
# can persist and conflict with the new wrapper headers.
install(CODE "file(REMOVE_RECURSE \"\${CMAKE_INSTALL_PREFIX}/include/dxrt\")")

# C ABI core headers
set(DXRT_PUBLIC_HEADERS
    ${DXRT_INCLUDE}/dxrt/dxrt_c_api.h
    ${DXRT_INCLUDE}/dxrt/dxrt_cxx_api.h
    ${DXRT_INCLUDE}/dxrt/gen.h
)

install(FILES ${DXRT_PUBLIC_HEADERS} DESTINATION include/dxrt)

# C++ API wrappers + backward-compat shims — all in wrapper/, installed flat
install(FILES
    ${DXRT_INCLUDE}/dxrt/wrapper/common.h
    ${DXRT_INCLUDE}/dxrt/wrapper/configuration.h
    ${DXRT_INCLUDE}/dxrt/wrapper/device.h
    ${DXRT_INCLUDE}/dxrt/wrapper/device_info_status.h
    ${DXRT_INCLUDE}/dxrt/wrapper/driver.h
    ${DXRT_INCLUDE}/dxrt/wrapper/dxrt_api.h
    ${DXRT_INCLUDE}/dxrt/wrapper/inference_engine.h
    ${DXRT_INCLUDE}/dxrt/wrapper/model.h
    ${DXRT_INCLUDE}/dxrt/wrapper/profiler.h
    ${DXRT_INCLUDE}/dxrt/wrapper/runtime_event_dispatcher.h
    ${DXRT_INCLUDE}/dxrt/wrapper/cli.h
    ${DXRT_INCLUDE}/dxrt/wrapper/datatype.h
    ${DXRT_INCLUDE}/dxrt/wrapper/device_compatibility_layer.h
    ${DXRT_INCLUDE}/dxrt/wrapper/exception_handler.h
    ${DXRT_INCLUDE}/dxrt/wrapper/inference_option.h
    ${DXRT_INCLUDE}/dxrt/wrapper/tensor.h
    ${DXRT_INCLUDE}/dxrt/wrapper/util.h
    DESTINATION include/dxrt
)

# Exception subdir
install(FILES
    ${DXRT_INCLUDE}/dxrt/wrapper/exception/exception.h
    DESTINATION include/dxrt/exception
)

# Extern vendored headers (cxxopts, rapidjson) — used by testbench
install(DIRECTORY ${CMAKE_SOURCE_DIR}/extern/include/dxrt/extern/
    DESTINATION include/dxrt/extern
    FILES_MATCHING PATTERN "*.h" PATTERN "*.hpp"
)

# CMake package config
install(FILES
    ${CMAKE_CURRENT_BINARY_DIR}/dxrtConfig.cmake
    ${CMAKE_CURRENT_BINARY_DIR}/dxrtConfigVersion.cmake
    DESTINATION lib/cmake/dxrt
)

# Install onnxruntime DLL to bin directory
if(MSVC AND USE_ORT)
    file(GLOB ORT_INSTALL_DLLS "${onnxruntime_LIB_DIRS}/*.dll")
    install(FILES ${ORT_INSTALL_DLLS} DESTINATION bin)
endif()

# ============================================================================
# dxrt_c: REMOVED — C ABI functions are now part of the main dxrt target.
# dxrt_c_api.cpp is compiled directly into libdxrt.so.
# ============================================================================
